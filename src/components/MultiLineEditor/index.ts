/**
 * MultiLineEditor - FreeFormEditor 多行编辑器
 * 
 * 仅保留 FreeFormEditor（用于 PlanManager）
 * MultiLineEditor 通用框架已移除，已被 PlanSlate 替代
 */

// 导出简化版编辑器（PlanManager 使用）
export { FreeFormEditor } from './FreeFormEditor';
export type { FreeFormEditorProps, FreeFormLine } from './FreeFormEditor';


