# 另一台电脑操作指南

## ✅ 问题已解决！

**最新更新：** 已将 TUI Calendar 的 dist 目录包含在仓库中，**不需要构建了**！

## 之前的问题

1. ~~Copilot 报告 `.gitmodules` 文件不存在，但 `tui.calendar` 被当做子模块提交了~~（已解决）
2. ~~dist 目录未包含在 git 中，需要构建~~（已解决）

## 解决方案

### 步骤 1: 删除旧的仓库（如果存在）
```powershell
cd到ReMarkable的父目录
Remove-Item ReMarkable -Recurse -Force
```

### 步骤 2: 重新克隆仓库
```powershell
git clone https://github.com/zoeysnowy/ReMarkable.git
cd ReMarkable
```

### 步骤 3: 验证 tui.calendar 是普通目录
```powershell
# 检查文件模式（应该是 100644，不是 160000）
git ls-files -s src/lib/tui.calendar | Select-Object -First 5

# 检查 .gitmodules 是否存在（应该不存在）
Test-Path .gitmodules

# 检查目录是否有内容（应该有很多文件）
Get-ChildItem src/lib/tui.calendar -Recurse | Measure-Object | Select-Object Count
```

## 预期结果

1. ✅ `.gitmodules` 文件**不存在**
2. ✅ `src/lib/tui.calendar` 目录下有大量文件（约680个文件）
3. ✅ `git ls-files -s` 显示文件模式为 `100644`（普通文件），不是 `160000`（子模块）
4. ✅ **NEW: `src/lib/tui.calendar/apps/calendar/dist/` 目录包含构建好的文件**

## 🎉 现在不需要构建了！

最新的提交 `1e07f54` 已经包含了预构建的 dist 文件：
- ✅ `toastui-calendar.js` (505KB)
- ✅ `toastui-calendar.mjs` (282KB) 
- ✅ `toastui-calendar.ie11.js` (972KB)
- ✅ `toastui-calendar.min.js` (183KB)
- ✅ `toastui-calendar.css` (31KB)
- ✅ `toastui-calendar.min.css` (26KB)

**克隆后即可直接使用，无需运行 `npm install` 和 `npm run build`！**

## 如果仍需要构建（不推荐）

如果你修改了 TUI Calendar 源码，才需要重新构建：

```powershell
cd src/lib/tui.calendar/apps/calendar
npm install
npm run build
```

## 如果问题依然存在

如果上述步骤后仍然有问题，执行：

```powershell
# 清理 git 缓存
git rm -r --cached .
git reset --hard HEAD

# 或者更彻底的重置
git fetch origin
git reset --hard origin/master
```

## 验证成功的标志

在另一台电脑上运行：
```powershell
git log -1 --oneline
```

应该显示：
```
1e07f54 feat: include TUI Calendar dist files in repository
```

验证 dist 目录存在：
```powershell
Test-Path src/lib/tui.calendar/apps/calendar/dist/toastui-calendar.js
# 应该返回 True
```

## 为什么会出现这个问题？

之前的提交 `60a40b0` 将 `tui.calendar` 作为子模块引用，但：
- 从未创建 `.gitmodules` 文件（正常的子模块应该有这个文件）
- 最新的提交 `310dab7` 已经将子模块转换为普通目录
- 另一台电脑的 Copilot 可能看到的是旧的缓存或未更新的状态

## 当前状态（此电脑）

```
✅ 已验证：.gitmodules 不存在
✅ 已验证：tui.calendar 是普通目录（681个文件）
✅ 已验证：文件模式为 100644（普通文件）
✅ 已验证：dist 目录已包含在 git 中（6个构建文件）
✅ 已推送：最新提交 1e07f54 已成功推送到 GitHub
```

## 更新日志

**2025-11-08 05:04** - 提交 `1e07f54`
- ✅ 将 TUI Calendar dist 目录添加到 git
- ✅ 修改 `.gitignore` 允许跟踪 `apps/calendar/dist/`
- ✅ 添加了所有预构建文件（约54MB）
- 🎉 **另一台电脑现在可以直接使用，无需构建！**

**2025-11-08 04:46** - 提交 `310dab7`
- ✅ 将 TUI Calendar 从子模块转换为集成目录
- ✅ 包含所有源代码和修改
