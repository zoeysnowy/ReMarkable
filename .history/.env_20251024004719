# React 开发服务器优化配置
# 这些设置会加快编译速度，但会牺牲一些开发时的功能

# 跳过 TypeScript 类型检查（开发时）
# 类型错误会在编辑器中显示，不影响开发
TSC_COMPILE_ON_ERROR=true

# 禁用 source maps（开发时）
# 会稍微降低调试体验，但编译快很多
GENERATE_SOURCEMAP=false

# 跳过预检查
SKIP_PREFLIGHT_CHECK=true

# 禁用 ESLint 警告覆盖层
ESLINT_NO_DEV_ERRORS=true

# 注意：生产构建时应该移除这些设置！
